{
  "name": "svgtofont-server",
  "version": "1.0.0",
  "description": "When you upload an svg image, it will be converted into an available font icon",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "font": "svgtofont --sources ./svg --output ./font --fontName uiw-font",
    "start": "node index.js",
    "clean": "npx rimraf font/* && npx rimraf svg/*"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.20.1",
    "express": "^4.18.2",
    "fs": "^0.0.1-security",
    "jszip": "^3.10.1",
    "path": "^0.12.7",
    "svgtofont": "^3.19.0"
  }
}
